[project]
name = "websockets"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.12.15",
]

[dependency-groups]
dev = [
    "websockets",
]

[tool.uv.sources]
websockets = { workspace = true }
